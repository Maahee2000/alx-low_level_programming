#include"main.h"

/**
 * main - causes an infinite loop.
 * Return: 0
 */
int main(void)
{
	int i;

	printf("infinit loop incomin :(\n");

	i = 0;

	while(i > 10)
	{
	i++; /*putchar(i) ;*/
	}

	printf("infinit loop avoided! \\o/\n");

	return (0);
}
